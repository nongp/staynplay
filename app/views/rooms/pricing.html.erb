<div class="row">
  <div class="col-md-3">
    <%= render 'room_menu' %>
  </div>
  <div class="col-md-9">
    <div class="panel panel-default">

      <div class="panel-heading">
        ราคา
      </div>

      <div class="panel-body">
        <div class="container">
          <%= form_for @room do |f| %>

            <div class="row">
              <div class="form-group">
                <label>ราคาต่อคืน</label>
                <%= f.text_field :price, placeholder: "เช่น: 3000", class: "form-control", required: true %>
              </div>
            </div>

            <div class="text-center">
              <%= f.submit "บันทึก", class: "btn btn-form" %>
            </div>

          <% end %>
        </div>
      </div>

    </div>
  </div>
</div>
